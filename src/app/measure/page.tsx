"use client";

import { MeasureFlow } from "@/components/measure/MeasureFlow";

/** 測定フロー(§3.1 / §4.3)。没入設計のため下部ナビは layout 側で非表示。 */
export default function MeasurePage() {
  return <MeasureFlow />;
}
