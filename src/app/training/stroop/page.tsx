"use client";

import { StroopTraining } from "@/components/training/StroopTraining";

/** トレオタ: 切替力トレーニング(ストループ × 50問)。DB保存なしの自由特訓。 */
export default function TrainingStroopPage() {
  return <StroopTraining />;
}
