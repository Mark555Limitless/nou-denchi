"use client";

import { MathTraining } from "@/components/training/MathTraining";

/** トレオタ: 処理速度トレーニング(単純計算 × 2分)。DB保存なしの自由特訓。 */
export default function TrainingMathPage() {
  return <MathTraining />;
}
